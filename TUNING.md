# 调参指南 — `main.py` 装甲板跟随

症状导向：车出什么问题 → 查表找到 **文件 + 行号 + 常量**，按建议方向改。
行号对应当前版本；若代码有增删，直接 **Ctrl+F 搜常量名** 定位。

## 0. 修改要求

1. **一次只改一个参数**，改完上车看效果，否则不知道是哪个起的作用
2. 每次调整幅度 **20%–50%**，不要一次翻倍
3. `main.py` 和 `debug_pose.py` 顶部的同名常量**必须保持一致**（识别相关：`MATCH_MIN_SCORE`/`SCALE_*`/`FOCAL_PX`/`ARMOR_*`/死区/增益；调试判定用：`Z_TARGET_M`）
4. 改任何值都不影响 AVC1 协议；协议只碰 `avc1_protocol.py`（一般永远不用动）
5. 桌面调试不想让车动：`main.py` 第 **40** 行 `SEND_UART = False`

## 1. 常量总表（`main.py`）

| 行号 | 常量 | 现值 | 管什么 |
|-----:|------|------|--------|
| 38 | `SEND_HZ` | 30 | 发送频率 Hz（契约 20–50，别动） |
| 40 | `SEND_UART` | True | 是否发串口 |
| 44 | `MATCH_W, MATCH_H` | 40, 30 | 匹配分辨率（越大越稳越卡） |
| 52 | `MATCH_MIN_SCORE` | 0.62 | 匹配门槛 |
| 53–55 | `SCALE_MIN/MAX/STEP` | 0.3 / 0.9 / 0.05 | 能认多远多近 |
| 60–61 | `ARMOR_W_M/H_M` | 0.08 | 板实尺（米），**必须等于模板框住的实物边长** |
| 62 | `FOCAL_PX` | 277.0 | 像素焦距，**决定距离准不准** |
| 64 | `Z_TARGET_M` | 0.2 | 想停稳的距离（米） |
| 66 | `X_DEAD_M` | 0.02 | 左右死区（米），死了才不修 |
| 67 | `Z_DEAD_M` | 0.04 | 前后死区（米） |
| 69 | `KP_LAT` | 1.2 | 左右力度 |
| 70 | `KP_RANGE` | 0.9 | 前后力度 |
| 71 | `SMOOTH_A` | 0.5 | 速度平滑（越大越钝） |
| 74–77 | `ROI_EXPAND / MAX_TRACK_LOST / FULL_SCAN_INTERVAL / SCALE_NEIGHBOR` | 1.8 / 3 / 8 / 2 | 跟踪加速（非协议） |

`debug_pose.py` 对应行号：`MATCH_MIN_SCORE`=30、`ARMOR_*`=36–37、`FOCAL_PX`=38、`Z_TARGET_M`=39、`X_DEAD_M`=40、`Z_DEAD_M`=41、`KP_*`=42–43。

## 2. 识别问题（框不出来 / 框错）

### 症状：板在画面里但不出绿框
- **第 52 行 `MATCH_MIN_SCORE`**：0.62 → 试 0.55、0.50（降 = 更容易认到，但误认风险↑）
- 还不行 → **第 53–55 行尺度**：板很远时把 `SCALE_MIN` 0.3 → 0.2；板很近/很大时把 `SCALE_MAX` 0.9 → 1.0
- 屏幕上板 < 1/10 画面宽或 > 3/4 画面宽时基本认不到，先挪车

### 症状：绿框闪现闪没（贴着门槛）
- **第 52 行** 略降：0.62 → 0.58
- 或 **第 71 行 `SMOOTH_A`** 0.5 → 0.65（只是把输出变稳，不解决识别本身）

### 症状：框到别的东西（误认）
- **第 52 行** 升：0.62 → 0.68（别超过 0.75，否则近距容易丢）
- 换更特征的模板图（`/root/target1.jpg`）

### 症状：转个角度就丢
- 模板匹配天生只认正面。斜 30° 以内调 `MATCH_MIN_SCORE` 能救，更大角度该换图或加多模板（要改代码，找视觉组再说）

## 3. 距离问题（停得远近 / 数字跳）

### 症状：`debug_pose.py` 里 Z 和卷尺差很多
- **标定焦距**：`debug_pose.py` 第 39 行上方设 `MEASURED_Z_M = 卷尺读数`，跑起来读屏底 `f*=...`
- 把 `f*` 写进 **两个文件的第 62/38 行 `FOCAL_PX`**，重跑验证 Z ≈ 卷尺
- 注意标定时板要**正对**镜头，斜着量出来就不对

### 症状：车停的位置不是你想要的距离
- **第 64 行 `Z_TARGET_M`**：想停更近就降（如 0.15），更远就升（如 0.3）
- 停得位置 ≈ Z_TARGET（误差 ≤ Z_DEAD）

### 症状：Z 数字乱跳（±30% 以上）
- 多数是**匹配框宽抖**：升 `MATCH_MIN_SCORE`、或 **第 44 行** `MATCH_W, MATCH_H = 80, 60`（匹配更细，帧率会降，`SEND_HZ` 可能要跟降到 25）
- **第 67 行 `Z_DEAD_M`** 0.04 → 0.06：跳得再厉害车也不动，治标

### 症状：板太近时认不到 / 距离算出来离谱
- **第 53 行 `SCALE_MAX`** 0.9 → 1.0，同时看 `debug_pose` 的 `w`：模板缩到 40×30 里最多 ~27px，板占画面 3/4 以上时该模板就不够用了

## 4. 左右问题

### 症状：板明明偏右，车不动或动错了方向
- 先看 `debug_pose`：`LR` 显示什么？
  - 显示 `RIGHT` 但车不动 → 板在死区内，**第 66 行 `X_DEAD_M`** 0.02 → 0.01
  - 方向反了 → **第 273 行** `vy = -KP_LAT * x_m` 把负号去掉/加上（改符号，不是改增益）
- 判定本身错（板在左显示 RIGHT）→ 识别问题，回到第 2 节

### 症状：左右振荡（车像钟摆）
- **第 69 行 `KP_LAT`** 1.2 → 0.8、0.6 往下降
- **第 66 行** 死区略加大 0.02 → 0.03
- **第 71 行 `SMOOTH_A`** 0.5 → 0.65（输出更钝）

### 症状：对中太慢/太肉
- **第 69 行** 1.2 → 1.5、1.8 往上升（接近 2.0 还肉就该查帧率是不是掉到 20 以下）

### 症状：中心附近左右小幅晃个不停
- **第 66 行 `X_DEAD_M`** 加大：0.02 → 0.03（死区内 vy 强制 0）

## 5. 前后问题

### 症状：跟太快，冲过头再倒回来
- **第 70 行 `KP_RANGE`** 0.9 → 0.6、0.5
- **第 67 行 `Z_DEAD_M`** 0.04 → 0.06

### 症状：跟太慢，板跑远了追不上
- **第 70 行** 0.9 → 1.2（板每远 1m 输出 1.2m/s）
- 不够就把 **第 37 行 `MAX_SPEED`** 上限用足（协议顶 0.8，现在就是 0.8，能提的只有增益）

### 症状：距离在死区边缘来回拉锯（进→退→进）
- **第 67 行 `Z_DEAD_M`** 0.04 → 0.07

### 首跑保守值（第一次上车建议先改成这个，方向对了再调回）
```
第 69 行  KP_LAT   = 0.6
第 70 行  KP_RANGE = 0.45
```

## 6. 跟丢 / 帧率问题

### 症状：轻微遮挡或目标快速移动后长时间 SEARCHING
- **第 74 行 `ROI_EXPAND`** 1.8 → 2.2（搜更大范围，稍慢）
- **第 75 行 `MAX_TRACK_LOST`** 3 → 2（更快放弃 ROI 回全图）

### 症状：帧率低（屏卡 / debug 里 `w` 更新慢）
- **第 44 行** 匹配分辨率是最大杠杆：40×30 快但糙；80×60 稳但可能只有 20fps
- 降 **第 38 行 `SEND_HZ`** 30 → 25 匹配实际帧率（契约下限 20）
- **第 76 行 `FULL_SCAN_INTERVAL`** 8 → 12（少做全图扫描）

### 症状：明明锁着却突然发停车（valid=0）
- 识别短暂丢 3 帧（`MAX_TRACK_LOST`）就发停车帧——这是**设计行为**（契约要求）。车会顿一下的话：升 `MATCH_MIN_SCORE` 的反面（降到能稳住）、或 **第 75 行** 3 → 5 多容忍几帧

## 7. 上下不动是对的

车只管**左右（vy）+ 前后（vx）**，画面里板偏高偏低**不会**引起任何运动——AVC1 没有上下轴，麦轮也升不了降。看到 Y 不为 0 不用管。

## 8. 完整标定 + 联调流程（从零开始按顺序做）

1. `debug_pose.py` 设 `MEASURED_Z_M` → 标定 `FOCAL_PX` 写回两文件
2. 量模板框住的白块实尺 → 核对 `ARMOR_W_M/H_M`（0.08 对不对）
3. 桌面跑 `debug_pose`：板左右挪，`LR` 判定全对；前后挪，`FAR/HOLD/NEAR` 正确
4. `main.py` 改保守增益（第 5 节末）上车
5. 静态：不按 L1 不动 → 松 L1 停 → 断串口 200ms 停
6. 按 L1 低速：先左右后前后，方向对了再放开
7. 方向/手感不对 → 回对应节调参，**每次一个**

## 9. 万能排查顺序

车不动：L1 按了吗 → 圈+方块进 auto 了吗 → 共地了吗 → `SEND_UART=True` 吗 → debug 里 `LR/CENTER` 了吗
车乱动：先降两个 KP → 再看是不是误认（`MATCH_MIN_SCORE` 升）→ 最后查 `FOCAL_PX`
