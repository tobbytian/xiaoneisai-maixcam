# main.py 调参指南

车出了什么毛病，到下面找对应的症状，按建议的方向改。行号对应当前版本的代码，改过代码就直接 Ctrl+F 搜常量名。

## 改之前

一次只改一个参数，改完上车看效果，不然说不清是哪个起的作用。每次调整幅度在两三成，最多一半，别一上来翻倍。

`main.py` 和 `debug_pose.py` 顶部的同名常量必须保持一致。识别相关的有 `MATCH_MIN_SCORE`、`SCALE_*`、`FOCAL_PX`、`ARMOR_*`、死区和增益，调试判定用的 `Z_TARGET_M` 也要一样。

改这些值碰不到 AVC1 协议。协议只在 `avc1_protocol.py` 里，一般永远不用动。

桌面调试不想让车动，把 main.py 第 40 行 `SEND_UART` 改成 False。

## 常量总表

| 行号 | 常量 | 现值 | 管什么 |
|-----|------|------|--------|
| 38 | `SEND_HZ` | 30 | 发送频率 Hz（契约 20-50，别动） |
| 40 | `SEND_UART` | True | 是否发串口 |
| 44 | `MATCH_W, MATCH_H` | 40, 30 | 匹配分辨率（越大越稳越卡） |
| 52 | `MATCH_MIN_SCORE` | 0.62 | 匹配门槛 |
| 53-55 | `SCALE_MIN/MAX/STEP` | 0.3 / 0.9 / 0.05 | 能认多远多近 |
| 60-61 | `ARMOR_W_M/H_M` | 0.08 | 板实尺（米），必须等于模板框住的实物边长 |
| 62 | `FOCAL_PX` | 277.0 | 像素焦距，决定距离准不准 |
| 64 | `Z_TARGET_M` | 0.2 | 想停稳的距离（米） |
| 66 | `X_DEAD_M` | 0.02 | 左右死区（米），死了才不修 |
| 67 | `Z_DEAD_M` | 0.04 | 前后死区（米） |
| 69 | `KP_LAT` | 1.2 | 左右力度 |
| 70 | `KP_RANGE` | 0.9 | 前后力度 |
| 71 | `SMOOTH_A` | 0.5 | 速度平滑（越大越钝） |
| 74-77 | `ROI_EXPAND / MAX_TRACK_LOST / FULL_SCAN_INTERVAL / SCALE_NEIGHBOR` | 1.8 / 3 / 8 / 2 | 跟踪加速（非协议） |

`debug_pose.py` 里同一批常量在 30 到 43 行，`MATCH_MIN_SCORE` 第 30 行，`ARMOR_W_M/H_M` 36 和 37，`FOCAL_PX` 38，`Z_TARGET_M` 39，死区 40 和 41，`KP_*` 42 和 43。

## 识别问题（框不出来、框错）

### 板在画面里但不出绿框

先把第 52 行 `MATCH_MIN_SCORE` 从 0.62 降到 0.55、0.50 试试。门槛低了容易认到，认错的风险也跟着大。还不行就动第 53-55 行的尺度，板很远把 `SCALE_MIN` 从 0.3 降到 0.2，板很近把 `SCALE_MAX` 从 0.9 升到 1.0。另外板占画面不足十分之一宽、或者超过四分之三的时候基本认不到，先把车挪到合适的距离。

### 绿框闪现闪没

贴着门槛了。第 52 行略降到 0.58，或者把第 71 行 `SMOOTH_A` 从 0.5 提到 0.65。后者只是让输出变稳，识别本身的问题还在。

### 框到别的东西了

第 52 行往上升，0.62 升到 0.68，别超过 0.75，不然近距离容易丢。也可以换张特征更明显的模板图。

### 车一转角度就丢

模板匹配天生只认正面，这个改参数救不了多少。斜 30 度以内还能靠降 `MATCH_MIN_SCORE` 硬撑，再大就得换模板或者加多模板，那要改代码，找视觉组。

## 距离问题（停得远近、数字跳）

### debug 里 Z 和卷尺差很多

焦距没标定过。debug_pose.py 第 39 行上方设 `MEASURED_Z_M`，值填卷尺读数，跑起来看屏底的 f* 建议值，把它写进两个文件的 `FOCAL_PX`（main.py 第 62 行、debug_pose.py 第 38 行），重跑验证 Z 和卷尺基本一致。标定的时候板要正对镜头，斜着量出来不对。

### 车停的距离不是想要的

第 64 行 `Z_TARGET_M`，想停近点就降，比如 0.15，想停远点就升，比如 0.3。最后停下的位置就在 Z_TARGET 附近，误差不超过 Z_DEAD。

### Z 数字乱跳

多半是匹配框宽度在抖。先升 `MATCH_MIN_SCORE`，或者把第 44 行 `MATCH_W, MATCH_H` 提到 80, 60，匹配更细，代价是帧率下降，`SEND_HZ` 可能要跟着降到 25。只想让车别跟着抖，把第 67 行 `Z_DEAD_M` 从 0.04 加到 0.06，这招治标不治本。

### 板太近认不到，或者距离算出来离谱

第 53 行 `SCALE_MAX` 升到 1.0。同时看 debug 里的 w 值，模板在 40×30 的匹配图里最多缩到 27px 左右，板占画面四分之三以上时这套模板就不够用了。

## 左右问题

### 板明明偏右，车不动或动错了方向

先看 debug_pose 里 LR 显示什么。

- 显示 RIGHT 但车不动，板在死区里，第 66 行 `X_DEAD_M` 从 0.02 降到 0.01
- 方向反了，去第 273 行看 `vy = -KP_LAT * x_m`，把负号去掉或加上，改符号，增益别动
- LR 本身就判错了（板在左显示 RIGHT），那是识别的问题，回上一节

### 左右振荡，车像钟摆

第 69 行 `KP_LAT` 从 1.2 往下降，0.8、0.6 这样试。死区也可以略加大，0.02 到 0.03。再配合第 71 行 `SMOOTH_A` 提到 0.65，输出钝一点。

### 对中太慢、太肉

第 69 行往上加，1.5、1.8 这样试。加到 2.0 还肉，先查帧率是不是掉到 20 以下了，帧率不够加增益也没用。

### 中心附近左右小幅晃个不停

第 66 行 `X_DEAD_M` 加大到 0.03。死区内 vy 强制为 0。

## 前后问题

### 跟太快，冲过头再倒回来

第 70 行 `KP_RANGE` 从 0.9 降到 0.6、0.5，第 67 行 `Z_DEAD_M` 加到 0.06。

### 跟太慢，板跑远了追不上

第 70 行升到 1.2，意思是板每远 1 米就多输出 1.2 m/s。还不够也没办法，协议顶格 0.8，第 37 行 `MAX_SPEED` 现在就是 0.8，能提的只有增益。

### 距离在死区边缘来回拉锯

第 67 行 `Z_DEAD_M` 加到 0.07。

### 第一次上车的保守值

方向还没验证过的话，建议先改成这个，跑顺了再往回调。

```
第 69 行  KP_LAT   = 0.6
第 70 行  KP_RANGE = 0.45
```

## 跟丢和帧率

### 轻微遮挡或目标快速移动后长时间 SEARCHING

第 74 行 `ROI_EXPAND` 加到 2.2，搜的范围大一点，稍微变慢。第 75 行 `MAX_TRACK_LOST` 从 3 降到 2，更快放弃 ROI 回全图找。

### 帧率低（屏卡，debug 里 w 更新慢）

第 44 行的匹配分辨率是最大的杠杆，40×30 快但糙，80×60 稳但可能只有 20fps。`SEND_HZ` 降到 25 去匹配实际帧率，别低于契约下限 20。第 76 行 `FULL_SCAN_INTERVAL` 从 8 加到 12，少做几次全图扫描。

### 明明锁着却突然发停车帧

识别连丢 3 帧（`MAX_TRACK_LOST`）就发 valid=0，这是设计行为，契约要求停车必须发出来。车会顿一下的话，把 `MATCH_MIN_SCORE` 降到能稳住的程度，或者第 75 行加到 5，多容忍几帧。

## 上下不动是对的

车只管左右和前后。画面里板偏高偏低不会引起任何运动，AVC1 没有上下轴，麦轮也升降不了。看到 Y 不为 0 不用管它。

## 从零开始的标定和联调

1. debug_pose.py 里设 `MEASURED_Z_M`，标定 `FOCAL_PX` 写回两个文件
2. 量一下模板框住的白块实际多大，核对 `ARMOR_W_M/H_M` 是不是 0.08
3. 桌面跑 debug_pose，板左右挪，LR 判定全对；前后挪，FAR/HOLD/NEAR 正确
4. main.py 换上保守增益（上一节末尾），上车
5. 静态检查，不按 L1 车不动，松 L1 停，断串口 200ms 停
6. 按 L1 低速试，先左右后前后，方向对了再放开
7. 手感不对回对应的小节调，记住一次只改一个

## 万能排查顺序

车不动。L1 按了吗，圆圈加方块进 auto 了吗，共地了吗，`SEND_UART` 是 True 吗，debug 里 LR 判定对吗。

车乱动。先把两个 KP 降下来，再查是不是误认（升 `MATCH_MIN_SCORE`），最后怀疑 `FOCAL_PX`。
